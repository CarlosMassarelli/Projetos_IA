def maximo(a,b):
    """
    Exercício USP Cursera

    verifica se um número é maior que o outro

    Args:
    a (int): O número a ser verificado.
    b (int): O número a ser verificado.

    Returns:
    retorna o número maior.
    """
    if a > b:
        return a
    else:
        return b

